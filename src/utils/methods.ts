// // Colors Properties
// import { useColor } from "./useColor";

// // Method for color
// export const getFontColor = (letter) => {
//   let color = useColor.filter((color) => color.letter === letter);
//   return color[0].fontColor;
// };

// // Get Background color
// export const getBackgroundColor = (letter) => {
//   let color = useColor.filter((color) => color.letter === letter);
//   return color[0].bgColor;
// };

// // Check duplicate
// export const checkDuplicateLetter = (text) => {
//   for (let i = 0; i < text.length; i++) {
//     for (let j = i + 1; j < text.length; j++) {
//       if (text[i] === text[j]) {
//         return true;
//       }
//     }
//   }
//   return false;
// };

export const ah = "";
